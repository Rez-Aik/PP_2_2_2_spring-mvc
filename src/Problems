1. Autowired старайся не над полем делать

(Добавил конструкторы и над ними поставил @Autowired)

2. над int count в контроллере неплохо было бы еще required=false поставить

(Добавил)

3. я не понял юмора с передачей списка машин в параметры метода.
Передавай только каунт по цепочке "контроллер - сервис - дао".
А вот в обратку уже список.
Метод оставь всего один и в сервисе, и в дао - getCarsByCount например

(Оставил по одному методу в сервисе и дао, без параметра списка)

4. Список инициализируется верно

5. в методе контроллера: cars переменная не нужна.

6. в дао: getCarsByCount строка 24 на выброс тоже,
метод limit успешно проглотит оба проверяемых кейса и не поперхнётся